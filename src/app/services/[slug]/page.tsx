import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Faq, SiteHeader } from "../../interactions";
import { ServiceVisual } from "../../media";
import { SiteFooter } from "../../site-footer";
import { getService,services } from "../service-data";

export function generateStaticParams(){return services.map(({slug})=>({slug}))}
export async function generateMetadata({params}:PageProps<"/services/[slug]">):Promise<Metadata>{const {slug}=await params;const service=getService(slug);if(!service)return {};const url=`/services/${service.slug}`;return{title:service.seoTitle,description:service.seoDescription,alternates:{canonical:url},openGraph:{title:service.seoTitle,description:service.seoDescription,url,type:"website"},twitter:{card:"summary_large_image",title:service.seoTitle,description:service.seoDescription}}}

export default async function ServicePage({params}:PageProps<"/services/[slug]">){const {slug}=await params;const service=getService(slug);if(!service)notFound();const index=services.findIndex(item=>item.slug===service.slug);const related=services.filter(item=>item.slug!==service.slug).slice(index%3,index%3+3);const faqItems=service.faqs.map(({question,answer})=>[question,answer]);const serviceSchema={"@context":"https://schema.org","@type":"Service",name:service.name,description:service.seoDescription,provider:{"@type":"Organization",name:"HP Techs"},areaServed:{"@type":"Country",name:"United Kingdom"},url:`/services/${service.slug}`};const breadcrumbSchema={"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:"/"},{"@type":"ListItem",position:2,name:"Services",item:"/services"},{"@type":"ListItem",position:3,name:service.name,item:`/services/${service.slug}`} ]};const faqSchema={"@context":"https://schema.org","@type":"FAQPage",mainEntity:service.faqs.map(item=>({"@type":"Question",name:item.question,acceptedAnswer:{"@type":"Answer",text:item.answer}}))};return <><a className="skip-link" href="#main">Skip to main content</a><SiteHeader/><main id="main"><section className={`service-detail-hero service-theme-${index+1}`}><div className="container service-detail-grid"><div><nav className="breadcrumb" aria-label="Breadcrumb"><Link href="/">Home</Link><span>/</span><Link href="/services">Services</Link><span>/</span><span aria-current="page">{service.name}</span></nav><p className="eyebrow">{service.eyebrow}</p><h1>{service.headline}</h1><p className="lede">{service.intro}</p><div className="actions"><Link className="button primary" href="/contact#enquiry-form">Discuss this service <span aria-hidden="true">↗</span></Link><a className="button secondary" href="#capabilities">See what is included</a></div></div><div className="service-hero-system"><ServiceVisual type={index+1}/><p>{service.signal}</p><div className="system-lines" aria-hidden="true"><i/><i/><i/><i/><i/></div><span>Service scope is confirmed after discovery</span></div></div></section><section className="service-problem section"><div className="container"><div className="split-heading"><div><p className="eyebrow">Where the pressure appears</p><h2>Technology problems rarely stay technical for long.</h2></div><p>The right response starts by understanding the operational effect, not simply treating the visible symptom.</p></div><div className="service-problem-grid">{service.problems.map(problem=><article key={problem.title}><i aria-hidden="true"/><h3>{problem.title}</h3><p>{problem.copy}</p></article>)}</div></div></section><section className="service-capabilities section" id="capabilities"><div className="container"><div className="service-capability-heading"><p className="eyebrow">How HP Techs can help</p><h2>Focused capability, connected to a clear outcome.</h2></div><div className="service-capability-grid">{service.capabilities.map((capability,capIndex)=><article key={capability.title}><span aria-hidden="true">{String(capIndex+1).padStart(2,"0")}</span><div><h3>{capability.title}</h3><p>{capability.copy}</p></div></article>)}</div></div></section><section className="service-delivery"><div className="container service-delivery-grid"><div><p className="eyebrow green">A practical scope</p><h2>What an engagement may include.</h2><p>Every item is subject to discovery, access, technical assessment and an agreed statement of work.</p></div><ul>{service.deliverables.map(item=><li key={item}><i aria-hidden="true"/>{item}</li>)}</ul></div></section><section className="service-outcomes section"><div className="container"><p className="eyebrow">The direction of travel</p><h2>Outcomes worth designing the service around.</h2><div className="service-outcome-grid">{service.outcomes.map(outcome=><article key={outcome}><i aria-hidden="true"/><h3>{outcome}</h3></article>)}</div><p className="service-qualification">Illustrative outcomes are not guarantees. Measures, responsibilities and success criteria should be agreed for each engagement.</p></div></section><section className="service-faq section"><div className="container faq-grid"><div><p className="eyebrow">Questions about {service.shortName}</p><h2>Useful detail before the first conversation.</h2><p>Final scope, availability and commercial terms are confirmed through consultation.</p></div><Faq items={faqItems}/></div></section><section className="related-services"><div className="container"><p className="eyebrow">Connected services</p><div className="related-service-grid">{related.map(item=><Link href={`/services/${item.slug}`} key={item.slug}><span>{item.name}</span><i aria-hidden="true">↗</i></Link>)}</div></div></section><section className="final-cta"><div className="container cta-grid"><div><p className="eyebrow green">Start with the operational need</p><h2>Let’s turn the technology pressure into a practical next step.</h2></div><div><p>Share what is happening, who it affects and what needs to improve.</p><div className="actions"><Link className="button light" href="/contact#enquiry-form">Start a conversation <span aria-hidden="true">↗</span></Link></div></div></div></section></main><SiteFooter/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify([serviceSchema,breadcrumbSchema,faqSchema])}}/></>}
